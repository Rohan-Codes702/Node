<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-zinc-900 flex items-center justify-center h-screen">
    <div class="w-full max-w-md bg-zinc-800 p-8 rounded-xl shadow-lg">
      <form method="post" action="/login">
        <h2 class="text-2xl text-white font-semibold text-center mb-6">
          Login
        </h2>
        <input
          type="email"
          placeholder="Email"
          name="email"
          class="w-full mb-4 p-3 rounded bg-zinc-900 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-indigo-500"
        />

        <input
          type="password"
          placeholder="Password"
          name="password"
          class="w-full mb-4 p-3 rounded bg-zinc-900 text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-indigo-500"
        />

        <button
          type="submit"
          class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-3 rounded transition"
        >
          Login
        </button>
      </form>
    </div>
  </body>
</html>
